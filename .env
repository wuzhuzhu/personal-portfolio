# This env file should be checked into source control
# This is the place for default values for all environments
# Values in `.env.local` and `.env.production` will override these values

# DATABASE_URL="postgres://eleme@localhost:5432/eleme"

DATABASE_URL="postgres://walter@localhost:5432/walter"

NEXT_PUBLIC_SITE_NAME='Walter Portfolio'
APP_ORIGIN='https://i.interview.run'
BLITZ_DEV_SERVER_ORIGIN='http://localhost:3000/'

# @next/bundle-analyzer
ANALYZE=false
